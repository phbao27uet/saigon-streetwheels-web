import { HomePage } from '@/components/features/home'

const Home = async () => {
  return (
    <div className="flex flex-col bg-transparent">
      <HomePage />
    </div>
  )
}

export default Home
